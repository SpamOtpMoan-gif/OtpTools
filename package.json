{
  "name": "otptools-build-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node workersgithub.js",
    "build": "echo \"Building ALL...\" && npm run build:debug && npm run build:release",
    "build:debug": "echo \"Building (debug) - simulated\"",
    "build:release": "echo \"Building (release) - simulated\"",
    "build:all": "npm run build:debug && npm run build:release"
  },
  "dependencies": {
    "node-telegram-bot-api": "^1.5.0"
  }
}
